package com.salisburyclan.launchpad.device;

import com.salisburyclan.launchpad.api.LaunchpadClient;

import javax.sound.midi.MidiDevice;
import javax.sound.midi.MidiUnavailableException;

/** Provides a LaunchpadClient. */
public interface ClientProvider {
  public boolean providesType(/*info*/);
  public LaunchpadClient getClient() throws MidiUnavailableException;
}
